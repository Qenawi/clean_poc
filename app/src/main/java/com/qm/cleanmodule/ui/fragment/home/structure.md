@Structure
------- FeatName ex: Home
     ----- domain -> [repo , usecase]
     ----- data   -> [server response , ui model]
     ----- viewModel -> viewModel + (vieModel ext if needed)
     ----- ui -> adapters , fragment , ui ext 
     ----- router , if needed 