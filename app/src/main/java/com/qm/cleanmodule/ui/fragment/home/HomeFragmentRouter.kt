package com.qm.cleanmodule.ui.fragment.home

import android.os.Bundle

// HomeFragmentRouter @Docs
object HomeFragmentRouter {
    fun openDetails(f: HomeFragment, bundle: Bundle) {
        // navigate to details screen
    }

    fun closeCurrent(f: HomeFragment) {
        // close current fragment / activity based on the need
    }

}